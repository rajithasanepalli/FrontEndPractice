 <table mat-table matSort [matSortDirection]="'desc'" [dataSource]="tableDataSource" class="table">

    <ng-container matColumnDef="title">
      <th mat-header-cell *matHeaderCellDef mat-sort-header class="titleHeader"><span>Title</span>
      <td mat-cell *matCellDef="let element" (click)="taskDetails(element);$event.stopPropagation()"> {{element.title}}
      </td>
    </ng-container>
    <ng-container matColumnDef="assignee">
      <th mat-header-cell *matHeaderCellDef mat-sort-header> Assignee </th>
      <td mat-cell *matCellDef="let element" (click)="taskDetails(element);$event.stopPropagation()">
        {{element.member_name}}
      </td>
    </ng-container>
    <ng-container matColumnDef="startDate">
      <th mat-header-cell *matHeaderCellDef mat-sort-header> Start Date </th>
      <td mat-cell *matCellDef="let element" (click)="taskDetails(element);$event.stopPropagation()">
        {{element.task_date | date: "MM/dd/yyyy"}}
      </td>
    </ng-container>
    <ng-container matColumnDef="dueDate">
      <th mat-header-cell *matHeaderCellDef mat-sort-header> Due Date </th>
      <td mat-cell *matCellDef="let element" (click)="taskDetails(element);$event.stopPropagation()">
        {{element.due_date | date:"MM/dd/yyyy"}}
      </td>
    </ng-container>

    <ng-container matColumnDef="status">
      <th mat-header-cell *matHeaderCellDef mat-sort-header>Status </th>
      <td mat-cell *matCellDef="let element" (click)="taskDetails(element);$event.stopPropagation()">{{element.status}}
      </td>
    </ng-container>

    <ng-container matColumnDef="action" *ngIf="claims.trustee">

      <th mat-header-cell *matHeaderCellDef> Action</th>
      <td mat-cell *matCellDef="let element">
        <span>
          <i [ngClass]="{'disable': !permissions.canEdit}" class="material-icons text-secondary asset-trash"
            (click)="editTask(element)">
            edit
          </i>
        </span>
        <span>
          <i [ngClass]="{'disable': !permissions.canDelete}" class="material-icons text-secondary "
            (click)="deleteTask(element)">
            delete_outline
          </i>
        </span>
      </td>

    </ng-container>
    <ng-container matColumnDef="isDataAvailable">
      <td mat-footer-cell *matFooterCellDef [attr.colspan]="displayedColumns.length" style="text-align: center;">
        No Tasks found
      </td>
    </ng-container>

    <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
    <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
    <tr mat-footer-row *matFooterRowDef="['isDataAvailable']"
      [ngStyle]="{'display':!(tableDataSource?.data?.length==0) ? 'none' : 'contents'}"></tr>
  </table>